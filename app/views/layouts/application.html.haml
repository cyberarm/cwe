!!!
%html
  %head
    <link href='http://fonts.googleapis.com/css?family=Ubuntu' rel='stylesheet' type='text/css'>
    %title= content_for?(:title) ? yield(:title) : "Coding WeekEnd"
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tag
    = yield(:head)
  %body
    #container
      %img{:src => "#{root_path}images/logo.png", :width => "33%"}
      #nav
        .button-group
          %a{:href => "/", :class => "button big icon home"} Home
          %a{:href => "/login", :class => "button big icon lock"} Login/Signup
          %a{:href => "/projects", :class => "button big icon chat"} Projects
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}"
      = yield